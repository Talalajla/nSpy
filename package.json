{
  "name": "nSpy",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron main.js",
    "minify-js": "for file in src/*.js; do terser $file -o ${file%.js}.min.js -c -m; done",
    "dist": "npm run minify-js && electron-builder"
  },
  "author": "Talalajla",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "gulp": "^4.0.2",
    "gulp-rename": "^2.0.0",
    "gulp-terser": "^2.1.0"
  },
  "devDependencies": {
    "electron": "^27.1.0",
    "electron-builder": "^22.14.5"
  },
  "build": {
    "appId": "com.example.yourapp",
    "win": {
      "target": "nsis"
    }
  }
}
